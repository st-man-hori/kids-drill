# architecture

## 概要

kids-drill は算数ドリル学習サービス。現時点のスコープは「小学1年生のたし算・ひき算」だが、将来的に学年（小1〜小6）・教科（算数・国語...）へ拡張予定。

## ターゲット

- 対象ユーザー: 小学生全般※サービス開始時のコンテンツは小学1年生の算数（たし算・ひき算）から
- 対応デバイス優先度: タブレット・スマホ ＞ PC（タッチファースト）

## 技術スタック

| 領域 | 採用技術 | 補足 |
|---|---|---|
| フロントエンド/フルスタック | Next.js v16 (App Router) + TypeScript | |
| スタイリング | Tailwind CSS | |
| アニメーション | Framer Motion | 正解演出など |
| ホスティング | Vercel (Hobbyプラン) | 非営利利用の間はHobbyでOK。 |
| DB | Neon (Postgres) ※Vercel Postgres経由 | スケールtoゼロだが自動復帰するため無料枠の一時停止問題を回避できる |
| ORM | Drizzle（想定） | Neonとの相性が良い |
| 認証 | NextAuth (Auth.js) Credentials Provider | 下記「認証設計」参照 |
| PWA | next-pwa | iPadホーム画面への追加に対応 |

## 認証設計

親アカウントという別レイヤーは持たず、アカウントは子どもごとの1つのみ。登録作業は親子一緒に行う（親が発行をサポートする）想定。

- 登録時にID（一意）とひみつのばんごう（6桁の数字PIN）を発行し、紙に書いて子どもに渡す運用
- PINは数字のみ・6桁。4桁＋レート制限でも実用上は十分（ATMのPINと同様の考え方）だが、親子で発行する認証情報として余裕を持たせて6桁に。英字混在にはしない（通常キーボード入力が必要になり、数字PINパッドUIの方針と矛盾するため）
- ID: DBで`UNIQUE`制約
- PIN: 平文保存せずbcryptでハッシュ化して保存
- ブルートフォース対策: Upstash Ratelimitでログイン試行回数を制限（IP/ID単位）
- ログイン失敗時、「IDが存在しない」か「PINが違う」かをレスポンスで区別しない（ID列挙対策）
- 入力UIはテキストキーボードではなく数字PINパッド（タッチ操作前提、6歳児でも操作しやすいUI）

## 公開・プライバシー方針

- リポジトリはパブリック公開予定
- 秘密情報（APIキー、DB接続文字列、NextAuthのシークレット等）は`.env.local`に置き`.gitignore`で除外し、ドキュメントにも記載しない

## 未決定・TODO

- ゲーム性の詳細 → [game-design.md](./game-design.md)
- テーブル設計 → [data-model.md](./data-model.md)
