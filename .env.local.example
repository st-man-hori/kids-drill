# Neon (Postgres) 接続文字列。Neonダッシュボードから取得
DATABASE_URL=

# NextAuth (Auth.js v5) のセッション署名用シークレット。`npx auth secret` で生成できる
AUTH_SECRET=

# 未実装: ログインのレート制限(docs/architecture.mdの認証設計)で使う予定
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
